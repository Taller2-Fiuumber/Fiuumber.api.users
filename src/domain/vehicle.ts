export class Vehicle {
  brand: string;
  model: string;
  image: string;

  constructor(brand: string, model: string, image: string) {
    this.brand = brand;
    this.model = model;
    this.image = image;
  }
}
